extends layout

block content
  h1= title
  form(method="POST" action="/new")
    h2 Enter your Text
    input(type="text" name="user" placeholder="user") 
    textarea(cols="30", rows="10" name="text" placeholder="text")
    button(type="submit") Submit
