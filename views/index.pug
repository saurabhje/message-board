extends layout

block content
  h1= title
  p Welcome to #{title}

  div.centered-messages
    each message in messages
      h2 #{message.user}
      p #{message.text}
      p #{message.added}

  a(href='/new') Create New Messages 